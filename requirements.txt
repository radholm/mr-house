# --- Core ---
numpy>=1.24
PyYAML>=6.0
python-dotenv>=1.0
# setuptools 81 removed pkg_resources, which webrtcvad imports at load time.
setuptools<81

# --- Audio I/O ---
sounddevice>=0.4.6      # mic capture + playback (needs PortAudio)
soundfile>=0.12         # wav decode/encode

# --- Wake word (local) ---
openwakeword>=0.6.0

# --- VAD (end-of-utterance) ---
# webrtcvad builds from source (needs a C compiler). On Windows use the prebuilt
# 'webrtcvad-wheels' so no MSVC build tools are required; both expose the same
# `import webrtcvad`.
webrtcvad>=2.0.10; platform_system != "Windows"
webrtcvad-wheels>=2.0.14; platform_system == "Windows"

# --- STT (local) ---
faster-whisper>=1.0.0
ai-edge-litert>=2.1.0

# --- TTS (local, custom voice) ---
piper-tts>=1.3.0        # 1.3+ uses the SynthesisConfig/AudioChunk API

# --- Voice FX ---
pedalboard>=0.9.0

# --- Brain ---
ollama>=0.3.0           # local LLM client
mcp>=1.0.0              # Model Context Protocol client
requests>=2.31         # local tools: web search + weather HTTP calls

# --- Display ---
pygame>=2.5
moderngl>=5.8

